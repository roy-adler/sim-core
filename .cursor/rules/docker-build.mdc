---
description: Docker-first builds, known warning cleanup, and intentional deferrals
alwaysApply: true
---

# Docker builds & warning hygiene

## How to build

- Prefer **Docker only** — do not install Node/Yarn/Rust/wasm-pack on the host unless the user asks.
- Dev image: `docker compose --profile development build hash-core-dev`
- Run: `docker compose --profile development up hash-core-dev` (port 3000 → 8080)
- Image target is `yarn-server` (`yarn serve:core`). Dockerfile builds `utils` + `engine-web` (WASM).

## What already works (keep working)

- WASM↔JS uses thin helpers in `packages/engine-web/rust/util.rs`:
  - `to_js_value` → `Serializer::json_compatible()` (not bare `to_value`)
  - `from_js_value` → `serde_wasm_bindgen::from_value`
- `Agent` deserializes with **`deserialize_map`**, not `deserialize_struct` + `BUILTIN_FIELDS`. serde-wasm-bindgen’s struct walker only visits declared fields and would drop custom keys (`decidedList`, `allMessages` → `null.concat` in behaviors). Do not revert that.
- Do not reintroduce deprecated `JsValue::from_serde` / `into_serde`, and do not add a `serde_json::Value` round-trip workaround unless a *new* type hits the same struct-field-list trap.
- `wasm-pack` is pinned in the Dockerfile to `0.15.0` via `cargo install`.
- `packages/engine-web/LICENSE.md` exists so wasm-pack stops complaining; Cargo `license` is `AGPL-3.0-only`. Do not set both `license` and `license-file`.
- Vendor `fux_kdtree` must keep **`edition = "2015"`**. `2018` breaks `use kdtree::...` paths. Visibility uses `pub(crate)` for private-module items.
- `docker-compose.yml` has **no** top-level `version:` (obsolete in Compose v2+).
- Yarn resolutions pin `@types/react` / `@types/react-dom` to 16.x; `tinygradient` is a direct core dependency for `gradient-path`.

## Intentionally remaining install noise

- Docker `yarn install --ignore-scripts` prints `Ignored scripts due to flag` — intentional for layer caching, not a dependency peer issue.
- Browser npm CLI “new major version” notices are unrelated to the workspace.

## Previously resolved peer warnings

Do not reintroduce these:

| Was | Fix |
|---|---|
| `@deck.gl/layers` vs `@loaders.gl/core` | Pin `@loaders.gl/core` to `2.3.0` |
| `vite-plugin-monaco-editor` vs monaco | `monaco-editor@0.33.0` |
| `cypress-iframe` unmet `@types/cypress` | Vendored package under `scripts/integration_tests/vendor/cypress-iframe` (no stub types peer) |
| sanddance `fluentui-react-cdn-typings` exact `@types/react@16.9.34` | Vendored typings with `^16.9.34` via yarn resolution |

## Yarn lockfile

If `package.json` deps change, refresh `yarn.lock` **inside Docker** (frozen-lockfile builds will fail otherwise), e.g.:

```bash
docker run --rm -v "$PWD":/app -w /app node:20-bookworm bash -lc \
  'corepack enable && corepack prepare yarn@1.22.22 --activate && yarn install --ignore-scripts'
```
